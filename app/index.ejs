<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>serialport-device</title>
    <meta name="viewport" content="initial-scale=1, maximum-scale=3, minimum-scale=1, user-scalable=no">
    <% if (htmlWebpackPlugin.options.appModules) { %>
      <!-- Add `app/node_modules` to global paths so `require` works properly in development -->
      <script>
        require('module').globalPaths.push('<%= htmlWebpackPlugin.options.appModules.replace(/\\/g,'\\\\') %>')
      </script>
    <% } %>


    <style>
      .loding-gif{
        background: #2d3a4b;
        position: absolute;
        width: 100%;
        height: 100%;
      }
      .logo-advanpro{
        position: absolute;
        left:0;
        right:0;
        top:0;
        bottom: 0;
        margin: auto;
      }
    </style>
  </head>
  <body>
    <div id="app">
      <div class="loding-gif">
        
      </div>
    </div>
    <!-- webpack builds are automatically injected -->
  </body>
</html>

